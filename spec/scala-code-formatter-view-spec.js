'use babel';

import ScalaCodeFormatterView from '../lib/scala-code-formatter-view';

describe('ScalaCodeFormatterView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
